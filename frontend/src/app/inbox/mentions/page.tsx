import { PlaceholderPage } from "@/components/shell/PlaceholderPage";

export default function MentionsPage() {
  return (
    <PlaceholderPage
      title="Mentions"
      subtitle="Where you were tagged. (UI scaffold)"
      primaryTitle="@ Mentions"
      primaryBody="A focused feed of items that require your attention."
      secondaryTitle="Coming soon"
      secondaryBody="We will connect this to notifications once Auth + roles are in place."
    />
  );
}

