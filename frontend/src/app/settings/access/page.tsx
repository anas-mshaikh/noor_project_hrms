import { PlaceholderPage } from "@/components/shell/PlaceholderPage";

export default function SettingsAccessPage() {
  return (
    <PlaceholderPage
      title="Roles & Access"
      subtitle="Permissions and access policies. (UI scaffold)"
      primaryTitle="Access Control"
      primaryBody="Define who can import data, manage HR, and adjust camera configurations."
      secondaryTitle="Coming soon"
      secondaryBody="We will integrate Firebase/Auth + server-side enforcement in a later phase."
    />
  );
}

