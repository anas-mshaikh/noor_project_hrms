import { PlaceholderPage } from "@/components/shell/PlaceholderPage";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      subtitle="Organization preferences. (UI scaffold)"
      primaryTitle="General"
      primaryBody="Global preferences that shape the whole product: defaults, thresholds, and UX."
      secondaryTitle="Coming soon"
      secondaryBody="Once roles are implemented, we will gate these settings appropriately."
    />
  );
}

