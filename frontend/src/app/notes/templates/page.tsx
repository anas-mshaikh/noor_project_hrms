import { PlaceholderPage } from "@/components/shell/PlaceholderPage";

export default function NotesTemplatesPage() {
  return (
    <PlaceholderPage
      title="Templates"
      subtitle="Reusable forms. (UI scaffold)"
      primaryTitle="Templates"
      primaryBody="Reusable note templates (daily checklist, incident report, onboarding form)."
      secondaryTitle="Coming soon"
      secondaryBody="We will add CRUD + sharing once we define roles and permissions."
    />
  );
}

