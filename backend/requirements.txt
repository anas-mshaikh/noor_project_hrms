fastapi>=0.115
uvicorn[standard]>=0.30
pydantic-settings>=2.0
email-validator>=2.1
PyJWT>=2.9
httpx>=0.27

SQLAlchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
pgvector>=0.3
python-multipart>=0.0.9
redis>=5.0
rq>=1.16
openpyxl>=3.1
firebase-admin>=6.5
requests>=2.32
pytest>=8.0

# --- HR (Phase 1): Resume parsing ---
# We keep this minimal: PDFs + DOCX are the common resume formats.
unstructured[pdf,docx]>=0.12.0

# --- HR (Phase 2): Text embeddings (HuggingFace) ---
# We use SentenceTransformers as a thin wrapper around HuggingFace models so we can
# switch models easily in the MVP (BGE-M3 now, others later).
sentence-transformers>=2.6.0
# FlagEmbedding currently expects Transformers 4.x internals.
# Unbounded installs can pull Transformers 5.x and break reranker imports.
transformers>=4.41,<5

# --- HR (Phase 3): Reranker (local) ---
# We use FlagEmbedding for BGE rerankers (cross-encoder style) to keep the pipeline
# fully local and to match the BGE ecosystem.
FlagEmbedding>=1.2.8

# --- ML ---
numpy>=1.26
# opencv-contrib is required for cv2.face (FacemarkLBF landmark alignment).
opencv-contrib-python>=4.10
ultralytics>=8.3
# Tracking helper used by Ultralytics ByteTrack (recommended)
lapx>=0.5.10
# Face recognition (ONNXRuntime + InsightFace/ArcFace ONNX models)
onnxruntime>=1.17

# Encryption
argon2-cffi >=23.1.0
